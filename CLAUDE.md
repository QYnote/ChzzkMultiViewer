# 프로젝트 지침

## 기능 개발 전 확인

신규 기능 개발 요청이 들어왔을 때, README.md의 `## 업데이트 예정 기능` 항목에 해당 기능이 등록되어 있지 않으면 개발을 바로 시작하지 않는다.

단, 다음 항목은 이 규칙에서 제외한다 (기능 개발이 아닌 프로젝트 관리로 간주):
- 리팩토링 / 코드 정리
- Git 관리 (커밋, 브랜치, 설정 등)
- 설정 파일 변경 (CLAUDE.md, manifest.json, settings 등)
- 문서 작업 (README.md 수정 등)

대신 다음과 같이 안내한다:

> "이 기능은 설계서(README.md)의 업데이트 예정 기능 목록에 등록되어 있지 않습니다. 개발 전에 설계서 Todo를 먼저 정리해 주세요."

이후 사용자가 README.md에 기능을 추가하거나, 명시적으로 진행 의사를 밝힌 경우에만 개발을 시작한다.

---

## Todo 관리 규칙

### 항목 추가 시
README.md의 `## 업데이트 예정 기능`에 항목을 추가한 경우, 개발 커밋과 별개로 해당 변경을 master에 바로 반영한다.

### 개발 완료 시 (feature → develop merge)
커밋 절차 3번에 따라 완료된 항목을 `[x]`로 표시한다.

### 배포 시 (develop → master)
배포 절차 3번에 따라 `[x]` 항목을 전부 삭제한다.

---

## 설계서 동기화

코드 작업 결과로 파일 추가/삭제/이동이 발생하거나 구조가 변경된 경우, 작업 완료 후 README.md의 다음 항목을 실제 상태에 맞게 업데이트한다:

- `## 파일 구조` — 파일 추가/삭제/이동 시
- `## 화면 구성` — UI 레이아웃 변경 시
- `## 주요 기능` — 기능 추가/제거/변경 시

`## 주요 기능`은 사용자 관점 위주로 작성하며, 별도 지시가 없으면 개발자 전용(내부 구현) 내용은 포함하지 않는다.

기능 항목의 세부 내용(하위 항목) 중 추가/변경 작업 시 함께 알고 있어야 할 동작 원리, 다른 기능과의 연관 관계, 제약사항 등이 있다면, 별도의 "개발 시 고려사항" 구분을 만들지 않고 관련된 항목 바로 아래에 한 단계 더 들여쓴 하위 목록으로 기록한다. 사용자가 이해할 수 있는 말로 작성하며, 코드 내부 구현 용어(파일명, 함수명, 변수명, CSS 클래스명 등)는 사용하지 않는다.

---

## Git 관리 전략

### 브랜치 구조

- **master** — 배포 전용 브랜치. 직접 커밋하지 않는다.
- **hotfix/설명** — master에서 파생. 긴급 수정 후 master와 develop 양쪽에 병합. 버전 0.0.1 단위 상승.
- **develop** — master에서 파생. 모든 개발의 기준 브랜치. 모든 파생 브랜치는 여기서 시작하고 여기로 병합된다.
- **feature/기능명** — develop에서 파생. 기능 개발 단위 브랜치. 소규모 기능도 반드시 feature 브랜치를 생성한다. 완료 판단 시 develop에 merge.

### 완료 판단 기준

사용자가 테스트 후 정상 동작을 확인한 경우를 완료로 간주한다.

### 커밋 절차 (사용자가 정상 동작 확인 시)

1. README.md 기준으로 이번 작업에서 개발된 내역을 사용자에게 보고한다.
2. 사용자가 확인하면 커밋을 진행한다.
3. README.md의 `## 업데이트 예정 기능`에서 완료된 항목을 `[x]`로 표시한다.
4. feature 브랜치를 develop에 merge한다.
5. merge 완료 후 feature 브랜치를 삭제한다.

### 버전 규칙

| 상황 | 단위 |
|------|------|
| hotfix 완료 후 배포 | +0.0.1 |
| 일반 기능 개발 배포 | +0.1.0 |

### 배포 절차

#### 1단계 — 배포 요청 시 ("배포하자")
1. 진행 중인 feature 브랜치가 있으면 그대로 두고 건드리지 않는다.
2. manifest.json의 version을 규칙에 맞게 올린다.
3. README.md의 `## 업데이트 예정 기능`에서 `[x]` 표시된 항목을 전부 삭제한다.
4. README.md의 `## 버전 수정 내역`에 새 버전 항목을 추가하고 변경사항을 기록한다.
   - 사용자 관점에서 체감할 수 있는 변화 위주로 작성한다.
   - `alert()`, `innerHTML`, `textContent` 등 내부 구현 용어는 사용하지 않는다.
   - 예: "입력창 특수문자 처리 개선", "알림 UI 개선" 등으로 표현한다.
5. develop → master 병합한다.
6. 병합된 변경사항을 사용자에게 요약 보고하고, push 여부를 확인해 달라고 안내한다.

#### 2단계 — 푸시 요청 시 ("푸시하자")
1. master를 origin/master에 push한다.
2. 배포 파일(ZIP)을 master 기준으로 생성한다.

### hotfix 절차

1. master에서 `hotfix/설명` 브랜치를 생성한다.
2. 긴급 수정을 완료한다.
3. master에 병합 후 버전 0.0.1 상승.
4. develop에도 동일하게 병합하여 수정 내용을 반영한다.
