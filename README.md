# MultiStream — Chrome 확장 프로그램

> **다루는 내용:** 설치·사용 방법과 설계 문서 트리 진입점
> **갱신 트리거:** 설치/사용 절차가 바뀌거나 하위 설계 문서 구성이 바뀔 때

치지직(Chzzk), SOOP 등 인터넷 방송 스트리머 여러 명을 동시에 한 화면에서 시청할 수 있는 Chrome MV3 확장 프로그램입니다.

> ⚠️ 공식 개발자 API를 사용하지 않습니다. 브라우저에 저장된 로그인 세션 쿠키 방식만 사용합니다.

## 하위 문서

| 문서 | 다루는 내용 |
|---|---|
| [Overview](document/Overview.md) | 기능 개요 (기능 영역별) |
| [Architecture](document/Architecture.md) | 계층 구성 · 의존 방향 · 통신 규칙 |
| [Build](document/Build.md) | 빌드/배포 환경 · 실패 대응 |
| [Git](document/Git.md) | Git 브랜치 전략 · 커밋/배포 절차 · 버전 규칙 |
| [Changelog](document/Changelog.md) | 버전 수정 내역 |

## 본문

### 설치 방법 (로컬 개발자 모드)

Chrome 웹스토어 출시 전 직접 설치 방법입니다.

1. 이 레포지토리를 다운로드(ZIP) 또는 클론합니다.
2. Chrome 주소창에 `chrome://extensions` 입력
3. 우측 상단 **개발자 모드** 토글 활성화
4. **압축해제된 확장 프로그램 로드** 클릭 → `source/` 폴더 선택
5. 확장 아이콘이 toolbar에 나타나면 완료

> 치지직에 로그인된 상태에서 사용해야 팔로잉 목록 연동 및 자동 로그인이 정상 동작합니다.

### 사용 방법

1. **스트리머 추가**
   - 팝업 → `스트리머 추가` 탭 → 치지직 / SOOP 플랫폼 선택
   - 직접 추가: `채널 고유 ID` 입력창과 `스트리머 이름` 입력창에 각각 입력 후 `시청 목록에 추가`
     - 치지직: 정확히 32자리 영문/숫자
     - SOOP: 방송 URL에서 확인하는 채널 ID
   - 또는 `내 팔로잉 목록 불러오기` 버튼으로 팔로잉 채널을 불러와서 추가

2. **멀티뷰 실행**
   - 팝업 → `시청 목록` 탭 → `멀티뷰 대시보드 열기`

3. **메인 화면 변경**
   - 팝업 `시청 목록`에서 서브 채널을 `📌 메인 화면` 박스로 드래그하거나,
     항목 메뉴(⋯) → `▶ 메인으로 지정`
   - 대시보드에서 서브 타일 클릭으로 실시간 스왑

4. **볼륨 조절**
   - 서브 타일에 마우스를 올리면 음소거 토글 + 볼륨 슬라이더 표시

### 업데이트 예정 기능

- [ ] 트위치(Twitch) 플랫폼 채널 멀티뷰 지원 추가
- [ ] 서브 채널과 메인 채널을 교환한 후 메인 채널의 영상이 재생되지 않는 버그 수정 — 재생/일시정지(space/k) 표시는 정상 동작하지만 영상 자체는 갱신되지 않는 문제
- [ ] 대시보드에서 메인↔서브를 교환한 뒤 서브 순서를 바꾸면 시청 목록이 깨지는 버그 수정 — 교환 결과가 시청 목록에 저장되지 않아, 순서 변경 시 교환 전 메인이 중복 저장되고 교환으로 메인이 된 채널이 목록에서 사라짐

**개발 제외 항목**

- 통나무 파워 자동 수령 — 채팅창에 수령 버튼이 나타나면 자동으로 클릭 (네이버에서 작업장(매크로)으로 판단할 수 있는 위험이 있어 보류)

### 일시적 오류 기록

재현이 되지 않아 확정 버그(업데이트 예정 기능)로 등록하지는 않았지만, 한 번 발생했던 오류를 날짜와 함께 기록합니다. 이후 새로운 버그가 발견되거나 Todo 항목을 추가할 때, 이 목록에 해당하는 내용이 있는지 먼저 확인합니다.

- **2026-08-02** — 서브 채널과 메인 채널 교환 후 메인 화면 영상이 갱신되지 않음 (재생/일시정지 단축키 표시는 정상 동작). 재현 안 됨 — 추정 원인: iframe을 DOM에서 다른 위치로 옮길 때(reparent) 발생하는 브라우저 비디오 컴포지팅 지연 가능성.

### 주의 및 면책

1. 본 확장프로그램은 Naver 및 Soop의 정보수집 방해 또는 삭제 요청 시 삭제될 수 있습니다.
2. 본 프로그램 개발 목적은 단순히 '여러 동시화면 보기 위함'이며 그 외의 목적은 없습니다.
3. 본 프로그램은 웹개발을 모르는 타언어 개발자가 Claude Code를 이용하여 개발하였습니다. 처음으로 AI만을 이용하여 개발을 시도해 본 프로젝트입니다.
